import s from './Header.module.css';

const Header = () => {
  return (
    <header className={s.header}>
      <h2 className={s.title}>Redux toolkit + Selectors</h2>
    </header>
  );
};
export default Header;
